name: Deploy to Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Deploy to Server
      uses: appleboy/ssh-action@master
      with:
        host: 143.198.26.90
        username: ${{ secrets.USERNAME }}
        password: ${{ secrets.PASSWORD }}
        port: 22
        script: |
          cd /root/devopss-hw
          git pull origin master  # Пример команды, чтобы получить изменения из репозитория
          # Добавь другие команды, которые нужно выполнить на сервере после обновления кода
